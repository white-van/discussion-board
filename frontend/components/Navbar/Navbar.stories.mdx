import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import { Navbar } from "./index";

<Meta title="Container/Navbar" component={Navbar} />

# Navbar

With `MDX` we can define a story for `Navbar` right in the middle of our
markdown documentation.

export const Template = (args) => <Navbar {...args} />;

<Canvas>
  <Story name="Navbar">{Template.bind({})}</Story>
</Canvas>

<ArgsTable of={Navbar} />
