import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import { HeroSection } from "./";

<Meta title="Component/HeroSection" component={HeroSection} />

# HeroSection

With `MDX` we can define a story for `HeroSection` right in the middle of our
markdown documentation.

export const Template = (args) => <HeroSection {...args} />;

<Canvas>
  <Story name="HeroSection">{Template.bind({})}</Story>
</Canvas>

<ArgsTable of={HeroSection} />
